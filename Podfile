# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'NumMemery' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for NumMemery

  pod 'IBAnimatable'

  pod 'ObjectMapper'
  
  target 'NumMemeryTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'NumMemeryUITests' do
    # Pods for testing
  end

end
